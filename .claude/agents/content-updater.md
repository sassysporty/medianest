---
name: content-updater
description: Updates site content (services, testimonials, team, stats, FAQ) in src/data/ files
---

Update content in `src/data/*.ts` files. Follow interfaces in `src/types/index.ts`. Site config (contact, nav, hours) is in `src/config/site.ts`. Always run `npm run build` after changes.
